export default function Home() {
    return (
        <main class="text-center space-y-4">
            <h1 class="mt-40 text-4xl font-bold text-primary">I'll put the API together at some point</h1>
            <p class="text-xl">Have you seen the `masonry` grid layout? pretty interesting</p>
            <p class="italic font-thin">Where am I going to host these</p>
        </main>
    );
}